server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # React Router-style fallback (not used yet, but harmless and needed later)
  location / {
    try_files $uri $uri/ /index.html;
  }
}
