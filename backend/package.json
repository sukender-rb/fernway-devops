{
  "name": "fernway-api",
  "version": "1.0.0",
  "description": "Fernway e-commerce backend API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "echo \"TODO: add real tests — this placeholder just keeps the pipeline green\" && exit 0",
    "migrate": "psql \"$DATABASE_URL\" -f db/schema.sql",
    "seed": "psql \"$DATABASE_URL\" -f db/seed.sql"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "pg": "^8.12.0",
    "prom-client": "^15.1.3"
  }
}
